# Target Firmware

Place the following ESP RAM binary in this directory:

- `app.bin` - RAM executable binary

This binary can be compiled from the test/target-example-src folder, or can be manually copied from your ESP-IDF build output.
